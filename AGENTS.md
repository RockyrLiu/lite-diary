# AGENTS.md — diary_lite

## 项目概述
Flutter 日记应用，主要运行场景为 Android 移动端。附带支持 Linux/Windows/Web，但非主要目标。

---

## 数据模型

### Entry（日记条目）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | int (PK) | 自增主键 |
| title | String? | 可选标题 |
| date | DateTime | 标注日期（未标注时默认创建日期） |
| content | String | Markdown 正文 |
| group_id | int (FK) | 所属分组（一个条目只能属于一个分组） |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 最后修改时间 |

### Group（分组）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | int (PK) | 自增主键 |
| name | String | 分组名称，唯一 |
| sort_order | int | 排序权重 |

默认分组：**日记**、**随笔**、**诗词**。用户可自行创建、删除、重命名分组。

### Tag（标签）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | int (PK) | 自增主键 |
| name | String | 标签名（关键词），唯一 |

### EntryTag（条目-标签关联）
| 字段 | 类型 | 说明 |
|------|------|------|
| entry_id | int (FK) | |
| tag_id | int (FK) | |

条目与标签为多对多关系。

### Image（图片）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | int (PK) | 自增主键 |
| entry_id | int (FK) | 所属条目 |
| file_path | String | 应用私有目录下的相对路径 |
| original_name | String | 原始文件名 |

### Settings（设置 — drift 表）
键值对形式存储加密密码的盐/哈希、WebDAV 配置等需要持久化的敏感设置。非敏感设置（外观偏好等）使用 `shared_preferences`。

---

## 功能范围详述

### 1. 日历视图 + 热力图
- 首页上半部分：GitHub 风格热力图（年度色块矩阵）
- 首页下半部分：传统月历视图
- 日历默认显示公历，农历月日以小字显示在公历日期下方
- 有日记的日期以圆点标记
- 热力图和日历均可点击跳转到对应日期的日记

### 2. Markdown 日记编辑器
- **渲染包**：`flutter_markdown`
- 按钮切换**源码模式**（可编辑）与**渲染模式**（纯预览），同一条日记内切换
- 不支持分屏
- 源码模式下提供 Markdown 工具栏（加粗、标题、列表、插入图片等快捷按钮）
- 支持插入图片：图片拷贝到应用私有目录下日记所属数据库目录中

### 3. 翻页查看
- **内容页**（查看/编辑）：左右滑动手势切换到前一个/后一个有内容的日期（全部分组，可按分组筛选）
- 同日多条目时，显示翻条按钮（仅在多条目时出现），点击在同日条目间切换
- **内容页默认展示当日日记的渲染模式**，可随时切换到源码模式编辑
- 右下角 `+` 按钮：补记日记（新增同日期的新条目，或新增其他日期的条目）

### 4. 文本分组
- 一个条目只属于一个分组（互斥）
- 默认分组：日记、随笔、诗词
- 支持创建、删除、重命名分组
- 分组页：通过首页**右滑手势**触达

### 5. 标签系统
- 标签类似关键词，条目与标签为多对多
- 在编辑器**顶部**有标签输入区：输入关键词 + 回车添加
- 支持从已有标签列表快速选择（自动补全）
- 支持创建、删除、重命名标签
- 支持 LLM 快速生成关键词标签（LLM 阶段实现）

### 6. 那年今日回顾
- 数据源：过去所有年份同一天的日记
- 通过首页**左滑手势**触达
- 同天多篇时随机展示一篇，划页查看其他
- 默认不依赖分组过滤，可在设置中配置过滤规则

### 7. 自动生成诗稿
- 依赖"诗词"分组下的条目
- 导出为单个 `.md` 文件
- 文件结构：
  - 一级标题：干支年 + 诗稿（如 `# 丙午诗稿`），年份按农历干支纪年
  - 目录（可跳转到正文对应位置）
  - 正文：按日期升序排列
    - 格式：`## {标题}  {农历月日}`（如 `## 静夜思  七月十三`）
    - 换行 + 正文（保持原 Markdown 格式）

### 8. LLM API 接入
- 用户自行提供 URL + API Key + 模型名称
- OpenAI API 格式（兼容任何实现此格式的后端）
- 对话页：通过 `+` 按钮按日期范围或逐条勾选日记作为上下文，然后提问
- 支持流式输出（SSE）
- 对话历史持久化存储
- API Key 用 `flutter_secure_storage` 纯本地存储

### 9. 数据导出
- 支持按分组或全部导出
- 格式：元数据文件（JSON）+ 打包文件（ZIP，含 Markdown 和图片）
- 导出到设备本地存储
- 可选择密文导出或明文导出

### 10. 搜索
- 位置：首页顶部、分组页顶部
- 支持按日期、标签、全文内容搜索
- **即时筛选**（用户输入时实时显示结果，无需点击搜索按钮）
- 应用运行时所有内容处于解密状态，搜索不受加密影响

### 11. 图片管理
- 支持在编辑器插入图片
- 图片拷贝到应用私有目录下按条目组织
- 图片随加密数据库一同加密
- 导出时图片打包进 ZIP
- 云同步时图片一同同步

---

## 导航与路由

### 首页（核心导航枢纽）
首页是一个 **PageView**，包含三页：
| 手势 | 页面 | 说明 |
|------|------|------|
| 左滑 | 那年今日页 | 展示往年同日日记 |
| 居中 | 首页 | 热力图 + 月历 |
| 右滑 | 分组页 | 按分组筛选/浏览日记 |

三页的顺序可在设置中自定义排序。

### 底部导航栏
从左到右：
| Tab | 页面 | go_router 路径 |
|-----|------|----------------|
| 首页 | HomePage（含三页 PageView） | `/` |
| 内容 | ContentPage（当日日记渲染视图） | `/content` 或 `/entry/:id` |
| LLM | LlmChatPage | `/llm` |
| 设置 | SettingsPage | `/settings` |

- 底部导航栏 tab 的**可见性和顺序**可在设置中自定义
- 内容页默认显示当日日记，通过翻页手势和底部 tab 均可到达

### 子页面路由
| 路径 | 页面 | 触发方式 |
|------|------|----------|
| `/entry/:id` | 内容查看/编辑页 | 日历/热力图点击、搜索点击 |
| `/entry/new` | 新建日记 | 右下角 `+` 按钮 |
| `/settings` | 设置主页 | 底部 tab |
| `/settings/cloud` | 云端配置 | 设置页进入 |
| `/settings/encryption` | 加密设置 | 设置页进入 |
| `/settings/llm` | LLM 配置 | 设置页进入 |
| `/settings/display` | 显示控制（tab 可见性/排序） | 设置页进入 |
| `/settings/about` | 关于 | 设置页进入 |
| `/llm` | LLM 对话列表 | 底部 tab |
| `/llm/:id` | 具体对话 | 对话列表进入 |
| `/export` | 数据导出（含诗稿导出） | 设置页进入 |

---

## 存储架构

### 加密策略
- **方案**：整库加密（`drift_sqlcipher`），数据库文件为密文
- **默认**：不加密（用户无感知）
- **启用**：用户在设置中设置密码，密码通过 PBKDF2 派生加密密钥
- **密码存储**：只存盐值和哈希（用于验证），密钥由密码运行时派生
- 忘记密码 = 数据不可恢复

### 云端存储
- **支持平台**：WebDAV（优先），百度网盘（可暂不实现）
- **同步策略**：增量同步（基于 updated_at 时间戳）
- **冲突解决**：多设备冲突时手动选择保留哪个版本
- **云端存储密文**：如果启用加密，云端存的就是密文（端到端加密）
- WebDAV 配置：用户提供 URL、账号、密码，存在 drift settings 表中

### 设置存储
| 存储方式 | 内容 |
|----------|------|
| `drift` settings 表 | 加密盐值/密码哈希、WebDAV 配置 |
| `shared_preferences` | 外观主题、导航栏可见性/排序、首页三页排序、那年今日过滤等非敏感项 |
| `flutter_secure_storage` | LLM API Key |

---

## 技术栈

| 用途 | 选型 |
|------|------|
| 状态管理 | Riverpod |
| 本地数据库（明文/加密） | drift / drift_sqlcipher |
| 路由 | go_router |
| Markdown 渲染 | flutter_markdown |
| 安全存储（API Key） | flutter_secure_storage |
| 非敏感设置 | shared_preferences |
| 农历日历 | `lunar_calendar` 或自建查表法（干支纪年 1900-2100） |
| 图片选择器 | image_picker |

---

## 实现顺序

**必须按此顺序逐步实现，切勿跳过或并行。每一阶段必须稳定可用后再进入下一阶段。**

### 阶段一：本地明文存储（MVP）
**范围**：除云端和 LLM 外的**所有功能**，包括：
- 日历视图 + 热力图 + 农历
- Markdown 编辑器（含工具栏、图片插入）
- 翻页查看（手势 + 同日多条目按钮）
- 分组 CRUD
- 标签 CRUD（含自动补全、创建/删除/重命名）
- 那年今日回顾
- 搜索（日期/标签/全文，即时筛选）
- 诗稿导出、数据导出
- 设置页（外观、导航栏自定义、那年今日过滤、关于）
- PageView 首页（三页）+ 底部导航栏（4 tab）
- 所有数据明文存储（drift，无加密）

> 阶段一功能已相当完整，建议内部再分 1a → 1b → 1c 子阶段：
> - **1a**：数据模型 + 基础 CRUD + 日历 + 编辑器 + 翻页
> - **1b**：分组 + 标签 + 搜索 + 那年今日
> - **1c**：图片 + 农历 + 导出 + 设置 + 导航自定义

### 阶段二：本地加密存储
- 引入 `drift_sqlcipher`，无感替换 drift
- 加密设置界面：设置/修改/移除密码
- 明文→密文数据迁移（首次加密时，用旧密钥读、新密钥写）
- 已有功能在加密下全量回归验证

### 阶段三：云端存储
- WebDAV 同步服务
- 增量同步（基于 updated_at）
- 手动冲突解决 UI
- 端到端加密验证（云端下载后无法直接读取）

---

## 代码组织

```
lib/
├── models/          # 数据模型（Dart 类 + drift 表定义）
├── database/        # drift 数据库、表、DAO
├── providers/       # Riverpod Provider
├── pages/           # go_router 页面
│   ├── home/        # 首页（含那年今日、分组页 PageView）
│   ├── content/     # 内容查看/编辑页
│   ├── llm/         # LLM 对话页
│   └── settings/    # 设置页及其子页
├── widgets/         # 可复用组件（日历、热力图、编辑器等）
├── services/        # 导出、农历、图片处理等服务
├── router.dart      # go_router 配置
└── main.dart
```

---

## 常用命令

```bash
flutter pub get                           # 安装依赖
flutter run                               # 运行到已连接设备
flutter test                              # 运行所有测试
flutter test test/foo_test.dart           # 运行单个测试
flutter build apk                         # 构建 release APK
flutter analyze                           # 静态分析（lint）
dart run build_runner build               # 生成 drift 代码
dart run build_runner watch               # 监控并自动生成 drift 代码
```

---

## 约束

- **没有 iOS 和 macOS 平台**，不要尝试构建。
- Flutter 使用预发布版本 (>=3.18.0-18.0.pre.54)，部分 package 可能不兼容。
- Android namespace: `com.example.diary_lite`（发布前需修改）。
- Gradle 使用 Kotlin DSL（`.kts`），Java 17。

## 测试

- `flutter test` 运行 `test/` 下所有测试。
- 实现新功能后，先将现有模板测试替换为对应功能的测试。
- 优先保证数据层（models/database）和业务逻辑层（providers/services）的单元测试覆盖率。

## 语言

所有用户界面文本、注释、文档使用中文。
